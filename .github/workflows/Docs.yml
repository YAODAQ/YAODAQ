name: 'docs'

on:
  push:
    branches:
      - main
    paths:
    - '!docs/**'
    - '!.github/workflows/**'
    - '.github/workflows/Docs.yml'
  pull_request:
    branches:
      - main
    paths:
    - '!docs/**'
    - '!.github/workflows/**'
    - '.github/workflows/Docs.yml'

env:
  CMAKE_VERSION: '3.16.x'
  CMAKE_BUILD_TYPE: 'Release'
  CMAKE_BUILD_PREFIX: './build'
  CMAKE_INSTALL_PREFIX: './install'

jobs:
  docs:
    runs-on: ubuntu-latest
    name: '📚 Docs'

    steps:

      - name: '🧰 Checkout'
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: '${{ matrix.icon }} Install packages'
        run: sudo apt-get install dia mscgen inkscape

      - name: '🐍 Setup conda'
        uses: s-weigand/setup-conda@v1
        with:
          update-conda: true
          activate-conda: true
          conda-channels: conda-forge

      - name: '🟨 Install packages with conda'
        run: conda install pip wheel doxygen graphviz sphinx sphinxcontrib-serializinghtml sphinxcontrib-jsmath sphinx-sitemap breathe sphinxcontrib-autodoc_doxygen sphinx-rtd-theme sphinxcontrib-htmlhelp sphinxcontrib-applehelp sphinxcontrib-devhelp sphinxcontrib-qthelp sphinxcontrib-websupport

      - name: '🟨 Install packages with pip'
        run: |
          pip install sphinxcontrib-moderncmakedomain

      - name: '🟨 Install miktex'
        run: |
          sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys D6BC243565B2087BC3F897C9277A7293F59E4889
          echo "deb http://miktex.org/download/ubuntu focal universe" | sudo tee /etc/apt/sources.list.d/miktex.list
          sudo apt-get update
          sudo apt-get install miktex
          sudo miktexsetup --shared=yes finish
          sudo initexmf --admin --set-config-value [MPM]AutoInstall=1

      - name: '🟨 Setup CMake'
        uses: jwlawson/actions-setup-cmake@v1.12
        with:
          cmake-version: '${{env.CMAKE_VERSION}}'
          github-api-token: ${{ secrets.GITHUB_TOKEN }}

      - name: '🔧 Configure'
        run: cmake -S . -B ${{ env.CMAKE_BUILD_PREFIX }} -DCMAKE_BUILD_TYPE=${{ env.CMAKE_BUILD_TYPE }} -DBUILD_DOCS=ON

      - name: '⚙️ Build'
        run: cmake --build ${{ env.CMAKE_BUILD_PREFIX }} --config ${{ env.CMAKE_BUILD_TYPE }} --parallel 2 --target all docs

      - name: '📥 Install'
        run: cmake --install ${{ env.CMAKE_BUILD_PREFIX }} --prefix ${{ env.CMAKE_INSTALL_PREFIX }} --strip
          && mv ${{ env.CMAKE_INSTALL_PREFIX }}/share/doc/YAODAQ/pdf/YAODAQ_Manual.pdf ${{ env.CMAKE_INSTALL_PREFIX }}/share/doc/YAODAQ/html/YAODAQ_Manual.pdf

      - name: '🎉 Deploy'
        uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          ACCESS_TOKEN: ${{ secrets.REPOS_ACCESS }}
          REPOSITORY_NAME: yaodaq/yaodaq.github.io
          BRANCH: main
          FOLDER: ${{ env.CMAKE_INSTALL_PREFIX }}/share/doc/YAODAQ/html/
          TARGET_FOLDER: YAODAQ
